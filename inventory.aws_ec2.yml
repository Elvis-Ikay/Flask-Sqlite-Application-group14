
plugin: amazon.aws.aws_ec2
regions:
  - eu-north-1
filters:
  "tag:project": flask-sqlite
keyed_groups:
  - key: tags.project
    prefix: project
  - key: tags.Project
    prefix: Project
  - key: tags.Name
    prefix: name
hostnames:
  - tag.Name

compose:
  ansible_host: public_ip_address
  
