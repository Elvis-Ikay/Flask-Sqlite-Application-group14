
plugin: amazon.aws.aws_ec2
regions:
  - eu-north-1
filters:
  tag:project: flask-sqlite
keyed_groups:
  - key: tags.Name
    prefix: name
  - key: tags.project
    prefix: project
hostnames:
  - private-ip-address
