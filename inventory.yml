---
plugin: aws_ec2
regions:
  - eu-north-1
filters:
  tag:Project: flask-sqlite
keyed_groups:
  - key: tags.Name
    prefix: name
  - key: tags.Project
    prefix: project
hostnames:
  - private-ip-address
